# /etc/mealie-mcp/secrets.env — root:root 0600
MEALIE_TOKEN=<long-lived API token of a dedicated non-admin Mealie user>
MCP_OAUTH_CLIENT_ID=mealie-mcp
MCP_OAUTH_CLIENT_SECRET=<client secret issued by your IdP for this server>
